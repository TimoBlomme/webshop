@using PE1.Webshop.Web.Enum
@model IEnumerable<PE1.Webshop.Web.Enum.Category>

@{
    ViewData["Title"] = "Categories";
}

<h2>Categories</h2>
<ul>
    @foreach (var category in Model)
    {
        <li>@category.GetHumanName()</li>
    }
</ul>
