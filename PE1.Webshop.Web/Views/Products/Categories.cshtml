@model IEnumerable<PE1.Webshop.Web.Enum.Category>

@{
    ViewData["Title"] = "Categories";
}

<h2>Categories</h2>
<ul>
    @foreach (var category in Model)
    {
        <li>@category.ToString()</li>
    }
</ul>
